


/**
 * Notes: No Implementation
 * Having as input a Process Object
 * Process Object will have the followiung: 
 *          System Clock 
 *          ID
 *          Frame Size
 *          Frame start 
 *          Frame End
 * 
 * Have Access to Ready Queue:
 *          Current Process
 *          List of Process
 */



 /**
  *   function:
  *     Take the last element of the Queue
        Process.startTimer()
        Sotre this result in boolean varibale: rusult 
        if(result){
            Remove from the Queue.remove(PCB); 
            //GUI.update()
        }else{
            Queue.remove(PCB);
            Queue.add(PCB);
            //GUI.update()
        }
  */